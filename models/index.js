const { Users } = require("./user");
const { Clients } = require("./client");
const { Approach } = require("./approach");
const { Platform } = require("./platform");

module.exports = { Users, Clients, Approach, Platform };
